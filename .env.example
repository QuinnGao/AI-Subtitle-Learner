# ============================================
# AI Subtitle Learner 环境变量配置示例
# ============================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# ============================================
# 基础配置
# ============================================
# 调试模式（True/False）
DEBUG=False

# 日志级别（DEBUG/INFO/WARNING/ERROR）
LOG_LEVEL=INFO

# 工作目录（存储处理中的文件）
WORK_DIR=./workspace

# 模型目录（存储 AI 模型）
MODEL_DIR=./models

# 日志目录
LOG_DIR=./logs

# ============================================
# LLM API 配置（用于字幕翻译、优化等功能）
# ============================================
# 推荐使用新格式（统一格式）
LLM_API_BASE=https://api.openai.com/v1
LLM_API_KEY=your-api-key-here
LLM_MODEL=gpt-4o-mini

# 兼容旧格式（可选，如果已设置新格式可忽略）
# 注意：系统会优先使用新格式，旧格式仅作为兼容保留
# OPENAI_API_BASE=https://api.openai.com/v1
# OPENAI_API_KEY=your-api-key-here
# OPENAI_MODEL=gpt-4o-mini

# ============================================
# Web 前端配置（用于 Docker Compose）
# ============================================
# API 基础 URL（前端访问后端 API 的地址）
# 本地开发：http://localhost:8000/api/v1
# Docker 环境：http://api:8000/api/v1 或使用反向代理的相对路径
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Node.js 环境（production/development）
NODE_ENV=production
